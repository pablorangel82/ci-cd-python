from setuptools import setup, find_packages

setup(
    name='my_module',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    author='Pablo',
    description='Exemplo de projeto Python com CI/CD',
    python_requires='>=3.7',
)
